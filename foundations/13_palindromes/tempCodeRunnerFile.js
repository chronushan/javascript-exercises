let arrayRev = [];
	// for (let i = array.length - 1; i >= 0; i--) {
	// 	if (array.charAt(-1) > 172 || array.charAt(-1) < 41) {
	// 		continue;
	// 	}
	// 	arrayRev.push(array[i]);
	// }
	// arrayRev = arrayRev.join("");

	// return lowerStr == arrayRev ? true : false;